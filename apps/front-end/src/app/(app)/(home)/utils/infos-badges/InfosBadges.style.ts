'use client'

import styled from 'styled-components'

export const InfosBadgesRoot = styled.div`
  display: flex;
  padding: 1.88rem 2rem;
  gap: 1rem;

  overflow-x: auto;

  &::-webkit-scrollbar {
    display: none;
  }
`
