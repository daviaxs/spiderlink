import { ReactNode } from 'react'
import { ScheduleHoursInputs } from './DaySchedule.style'

export function DayScheduleHoursInputs({ children }: { children: ReactNode }) {
  return <ScheduleHoursInputs>{children}</ScheduleHoursInputs>
}
